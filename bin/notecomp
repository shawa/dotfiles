#!/bin/bash

readonly STYLESHEET="$HOME/.pandoc/github.css"

pandoc -o ./index.html \
    --mathjax --toc -c "$STYLESHEET" \
    *.md metadata.yaml &

pandoc -o ../course.pdf \
    --toc --latex-engine=xelatex\
    ./*.md metadata.yaml &

