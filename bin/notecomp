#!/bin/bash

readonly STYLESHEET="$HOME/.pandoc/latex.css"

pandoc -o ./index.html \
    --mathjax --toc -c "$STYLESHEET" \
    *.md metadata.yaml &

pandoc -o ../course.pdf \
    --toc --latex-engine=xelatex \
	--number-sections \
    ./*.md metadata.yaml &

